sqlobject
{
	表的对象封装 表名->类名
	表字段的属性映射
	表对象生成表sql
	表字段属性的变更变更相应的表结构
	表的CRUD操作
	单行记录的对象映射以及操作(属性变更,创建,删除等)

	关联表(1:1 1:n n:n) 模式的实现
}

鉴于 表 到 对象,对象到表的映射过于复杂,不可控性太多,故屏蔽掉
从 表 到对象的映射源头,也就是说不支持从 数据库表结构中自动生成
sqlobject 对象.因为没有必要,再加上各个表自身一些额外字段,外键字段
不好控制.

Monitor: {

	表结构的构建和调整
	createTable(sqlobject)
	updateTable(sqlobject)


	
}