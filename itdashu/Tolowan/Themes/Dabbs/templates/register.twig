{% extends "Themes/Dabbs/templates/pageSingle.twig" %}
{% block body %}
    <div class="content-wrap">
        <div class="content">
            <div class="title">
                <h3>
                    <i class="fa fa-user-plus"></i>注册
                </h3>
                <span class="hehe"><a href="{{ url(['for':'login']) }}">登陆</a>或者<a href="{{ url(['for':'password']) }}">找回密码</a></span>
            </div>
            <hr>
            {{ content() }}

            <div class="form-one login-body">
                {{ view.r(data) }}
            </div>
        </div>
    </div>
{% endblock %} {% block bottom %}
{% endblock %}
