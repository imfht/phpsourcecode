<?php

require_once 'APKParser/res/AXMLParser.php';
			    
$stream = new APKParser\DataInputStream($file);
$parser = new APKParser\AXMLParser($stream);
// 				$info['line']=$parser->getApkInfos(array('manifest','application','uses-sdk','uses-permission'));

//获取指定的数据
$manifest = $parser->getApkInfos( array('manifest','uses-sdk') );


//获取全部数据
$manifest = $parser->getApkInfos();


?>