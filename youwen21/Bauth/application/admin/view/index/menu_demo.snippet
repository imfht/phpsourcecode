<?php
class demo{
    // 菜单模块使用案例
    public function menu_demo()
    {
        $module = 'admin';
        $menu = new Menu(true);
        $list = $menu->getMenu($module);
        $this->assign('list', $list);
        return $this->fetch();
    }
}