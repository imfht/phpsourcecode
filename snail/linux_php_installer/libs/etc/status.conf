location /check_status {
	check_status;
	access_log   off;
	allow all;
	#allow SOME.IP.ADD.RESS;
	#deny all;
}
location /nginx_status {
	stub_status on;
	access_log   off;
	allow all;
	#allow SOME.IP.ADD.RESS;
	#deny all;
}