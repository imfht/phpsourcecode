# lamp 下.htaccess 配置
RewriteEngine on

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

RewriteCond $1 !^(index\.php|images|robots\.txt)
RewriteRule ^(.*)$ /index.php/$1 [L]

# 注意，如果ci的system下目录里面有.htaccess deny from all ,请注释掉，具体原因可查看下方
# 传送门：http://my.oschina.net/u/1992642/blog/324016