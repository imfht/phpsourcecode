<div class="content-wrapper">

    <section class="content-header">
        <?=$this->partial('layout/breadcrumb')?>

        <h1 class="pull-right">
            <a href="<?=$this->url('supplier-category', ['action'=> 'add']);?>" class="btn btn-primary btn-sm"><i class="fa fa-plus"></i> <?=$this->translate('添加供应商分类')?></a>
            <?=$this->HelpUrl('supplierCategoryList')?>
        </h1>

    </section>

    <section class="content">
        <?=$this->partial('layout/messages'); ?>

        <div class="row">
            <div class="col-xs-12">
                <div class="box erp-box">

                    <form method="post" action="<?=$this->url('supplier-category', ['action' => 'updateAll']);?>" name="edit-all-form" id="edit-all-form">
                    <div class="box-body table-responsive no-padding">
                            <table class="table table-hover">
                                <thead>
                                <tr class="erp-bg-thead">
                                    <th width="2%"><input type="checkbox" class="minimal select-all-checkbox"></th>
                                    <th width="5%">ID</th>
                                    <th width="12%"><?=$this->translate('分类编码')?></th>
                                    <th><?=$this->translate('分类名称')?></th>
                                    <th width="8%"><?=$this->translate('排序')?></th>
                                    <th width="18%"><?=$this->translate('操作')?></th>
                                </tr>
                                </thead>

                                <?php if($this->supplierCategory) foreach ($this->supplierCategory as $category) { ?>
                                    <tr>
                                        <td><input  name="select_id[]" value="<?=$category->getSupplierCategoryId()?>" type="checkbox" class="minimal"></td>
                                        <td><?= $category->getSupplierCategoryId()?></td>
                                        <td><?= $this->escapeHtml($category->getSupplierCategoryCode())?></td>
                                        <td><?= $this->escapeHtml($category->getSupplierCategoryName())?></td>
                                        <td><input type="text" name="supplier_category_sort[<?=$category->getSupplierCategoryId()?>]" value="<?= $category->getSupplierCategorySort()?>" class="form-control input-sm"></td>
                                        <td>
                                            <a href="<?= $this->url('supplier-category',
                                                ['action'=>'edit', 'id'=>$category->getSupplierCategoryId()]); ?>"
                                               class="btn btn-xs btn-success"><i class="fa fa-pencil"></i> <?=$this->translate('编辑')?>
                                            </a>

                                            <a href="javascript:;" onclick="deleteConfirm('<?php echo $this->translate('您确实要删除该供应商分类吗？'); ?>', '<?= $this->url('supplier-category', ['action'=>'delete', 'id'=>$category->getSupplierCategoryId()], ['query' => ['qToken' => $this->adminHelper()->getCsrfValue()]]); ?>');" class="btn btn-xs btn-danger">
                                                <i class="fa fa-trash"></i> <?=$this->translate('删除')?>
                                            </a>

                                        </td>
                                    </tr>
                                <?php } ?>
                            </table>
                    </div>
                    <div class="box-footer clearfix">
                        <input type="checkbox" class="minimal select-all-checkbox"> <?=$this->translate('全选')?>
                        <select name="editAllState">
                            <option value=""><?=$this->translate('选择状态')?></option>
                            <option value="sort"><?=$this->translate('更新排序')?></option>
                        </select>
                        <button type="submit" class="btn btn-primary btn-sm"><i class="fa fa-check"></i> <?=$this->translate('更新')?></button>
                    </div>
                    </form>
                </div>

            </div>
        </div>

    </section>
</div>